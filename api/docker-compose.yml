services:
  postgres:
    image: postgres:16
    container_name: portfolio_starter_postgres
    environment:
      POSTGRES_DB: portfolio_starter
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
